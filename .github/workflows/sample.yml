name: first

on:
  push: 
    branches: python_sample

jobs:
 ubuntu:
  runs-on: ubuntu-latest
  steps:
  - run: date
  
 windows:
  runs-on: window-latest
  steps:
  - run: date      

 mac:
  runs-on: macos-latest
  steps:
  - run: date
  
 overall:
  needs: [ubuntu, windows, mac]
  runs-on: ubuntu-latest
  steps:
  - run: date
